class Ball {
  constructor() {
    this.position 
    this.velocity
  }
  
  collision(){
    // handle the case where objects to collide
  }
  update() {
    // Add the current speed to the position.
   //update speed
  }
  display() {
    // Display circle at x position
    stroke(0);
    fill(175);
    ellipse(__,__, 48, 48);
  }
}
